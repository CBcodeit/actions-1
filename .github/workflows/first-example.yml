name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome Message
              run: echo "Hello world!"

            - name: list files
              run: ls

            - name: read file
              run: cat Readme.md

